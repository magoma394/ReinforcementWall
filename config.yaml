# ReinforceWall Configuration File
# This file allows flexible configuration without modifying code

# Reward Configuration
rewards:
  block_attack: 10.0          # Reward for successfully blocking an attack
  log_attack: 1.0             # Reward for logging/alerting an attack
  block_legitimate: -5.0      # Penalty for blocking legitimate traffic (false positive)
  ignore_attack: -2.0         # Penalty for ignoring an attack (false negative)
  ignore_legitimate: -1.0     # Minor penalty for ignoring legitimate traffic

# Environment Configuration
environment:
  max_episode_steps: 100      # Maximum steps per episode
  state_dimension: 15         # Number of features in state vector
  num_actions: 4              # Number of possible actions (block, alert, log, ignore)

# Attack Simulation Configuration
attacks:
  prob_sql_injection: 0.07    # Probability of SQL injection attack per timestep
  prob_xss: 0.07              # Probability of XSS attack per timestep
  prob_brute_force: 0.08      # Probability of brute force attack per timestep
  prob_ddos: 0.07             # Probability of DDoS attack per timestep
  prob_command_injection: 0.05 # Probability of command injection attack per timestep
  prob_path_traversal: 0.04   # Probability of path traversal attack per timestep
  prob_port_scanning: 0.04    # Probability of port scanning attack per timestep
  prob_csrf: 0.03             # Probability of CSRF attack per timestep
  prob_mitm: 0.02             # Probability of MITM attack per timestep
  prob_phishing: 0.03         # Probability of phishing attack per timestep
  prob_normal: 0.50           # Probability of normal traffic
  
  # Attack intensity
  sql_injection_requests: 5   # Number of requests per SQL injection attack
  xss_requests: 3             # Number of requests per XSS attack
  brute_force_requests: 10    # Number of requests per brute force attack
  ddos_requests: 50           # Number of requests per DDoS attack
  ddos_num_ips: 10            # Number of IPs involved in DDoS attack
  command_injection_requests: 4  # Number of requests per command injection attack
  path_traversal_requests: 6  # Number of requests per path traversal attack
  port_scanning_requests: 20  # Number of requests per port scanning attack
  port_scanning_ports: [80, 443, 8080, 8443, 22, 21, 25, 3306, 5432, 6379]  # Ports to scan
  csrf_requests: 3            # Number of requests per CSRF attack
  mitm_requests: 5            # Number of requests per MITM attack
  phishing_requests: 4        # Number of requests per phishing attack
  
  # Time window for request frequency calculation (seconds)
  time_window_seconds: 60

# Firewall Configuration
firewall:
  simulation_mode: true        # If true, simulate actions without real iptables (safe for demos)
  log_dir: "data/logs"        # Directory for firewall logs
  alert_log_file: "data/logs/alerts.log"
  traffic_log_file: "data/logs/traffic.log"
  
  # iptables settings (only used if simulation_mode is false)
  iptables_chain: "INPUT"
  iptables_table: "filter"

# Training Configuration (for future DQN implementation)
training:
  learning_rate: 0.001
  batch_size: 32
  memory_size: 10000
  gamma: 0.95                 # Discount factor
  epsilon_start: 1.0          # Initial exploration rate
  epsilon_end: 0.01          # Final exploration rate
  epsilon_decay: 0.995       # Exploration decay rate

# Metrics Configuration
metrics:
  output_dir: "data/metrics"
  export_format: ["json", "csv"]  # Export formats for metrics

# Logging Configuration
logging:
  level: "INFO"               # Logging level: DEBUG, INFO, WARNING, ERROR
  log_dir: "data/logs"
  console_output: true        # Print logs to console

